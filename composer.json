{
    "require": {
        "pengstrom/pdfprint-lib": "^1.0",
        "raveren/kint": "^1.0",
        "symfony/yaml": "^2.7"
    },
    "scripts": {
        "post-install-cmd": [
            "bower install"
        ],
        "post-update-cmd": [
            "bower install"
        ]
    }
}
