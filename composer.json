{
    "require": {
        "raveren/kint": "^1.0",
        "symfony/yaml": "^2.7",
        "pengstrom/pdfprint-lib": "^1.1"
    },
    "scripts": {
        "post-install-cmd": [
            "bower install"
        ],
        "post-update-cmd": [
            "bower install"
        ]
    }
}
