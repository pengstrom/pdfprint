{
    "require": {
        "raveren/kint": "^1.0",
        "symfony/yaml": "^2.7",
        "pengstrom/pdfprint-lib": "^1.3"
    },
    "scripts": {
        "post-install-cmd": [
            "bower install"
        ],
        "post-update-cmd": [
            "bower install"
        ]
    }
}
